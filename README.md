### BetsuBetsu-service

Backend for BetsuBetsu-service. This simple backend service is developed using Express.js and uses a MongoDB database connection in the cloud. It is deployed using Render.

The user login data is stored in the MongoDB database connection.
